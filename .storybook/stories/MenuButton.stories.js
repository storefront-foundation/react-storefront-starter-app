import React from 'react'
import { MenuButton } from 'react-storefront/menu'

export default { title: 'MenuButton' }

export const closed = () => <MenuButton />
export const open = () => <MenuButton open />
